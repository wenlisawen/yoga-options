/*
const API_HOST = "https://localhost"

const config = {
  api_url_product: `${API_HOST}/product`,
  api_url_user: `${API_HOST}/user`,
  api_url_login: `${API_HOST}/login`,
  api_url_logout: `${API_HOST}/logout`,
};
*/

// Fake API for testing
const API_HOST = "https://course-api.com";

const config = {
  api_url_product: `${API_HOST}/react-store-products`,
  api_url_user: `${API_HOST}/user`,
  api_url_login: `${API_HOST}/login`,
  api_url_logout: `${API_HOST}/logout`,
};

export default config;
